# Дополнительное задание
b = input('Введите текст: ')
if len(b) >= 3:
    # Первое задание
    print(b[:3])
    # Второе задание
    print(b[-3:])
    # Третье задание
    print(b[1:-1:2])
    if len(b) == 0:
        print('Вы забыли ввести текст.')
else:
    print(b)
# Четвертое задание
print(b[0] + b[-1] + b[-1:1:-1])
# Пятое задание
print(b[0:2] + b[-2:])
if len(b) <= 2:
    return b
                                      #ДОДЕЛАТЬ!!!
